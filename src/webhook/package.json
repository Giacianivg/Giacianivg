{
  "name": "pousada-whatsapp-webhook",
  "version": "1.0.0",
  "description": "WhatsApp Business API webhook handler — Pousada Luz da Lua",
  "main": "handler.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node handler.js",
    "dev": "node --watch handler.js",
    "test": "node --test tests/handler.test.js",
    "lint": "npx eslint . --ext .js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "keywords": [
    "whatsapp",
    "webhook",
    "meta",
    "pousada"
  ],
  "license": "UNLICENSED"
}
